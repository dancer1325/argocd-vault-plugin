apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: avp-test
  namespace: argocd
spec:
  destination:
    namespace: default
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: 'https://github.com/dancer1325/argocd-vault-plugin'
    targetRevision: HEAD
    path: docs/examples/config
    # plugin: {}
    #   Argo CD AUTODETECT the plugin
    plugin: {}
